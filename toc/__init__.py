"""ToC (TikTok Story Creator) Python modules.

This repository is primarily spec-first (docs + scripts). The `toc/` package hosts
minimal reusable code, such as API clients, that scripts can share.
"""

