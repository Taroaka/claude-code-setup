# Tasklist: Video Tool Routing (Veo/Kling)

1. Add `--video-tool` to manifest scaffolding scripts.
2. Update manifest templates to mention `kling_3_0`.
3. Add Kling branch to `scripts/generate-assets-from-manifest.py`.
4. Update agent/docs so the workflow knows the new option.
5. Run compile checks.
