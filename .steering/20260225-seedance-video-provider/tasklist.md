# Tasklist: Seedance (BytePlus ModelArk) Video Provider

1. Add `toc/providers/seedance.py` (submit + poll + download, data-url image encoding).
2. Wire `scripts/generate-assets-from-manifest.py` to support Seedance video tool routing and CLI/env config.
3. Update manifest templates + run docs to mention Seedance video option and env vars.
4. Run `python -m compileall .` (or targeted compile) and a dry-run example.

